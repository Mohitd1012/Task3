SPLUNK
have operational intelligence
install(rpm -ivh splunk)
cd /opt
cd /splunk(home directory)
cd /bin
commands of splunk
terminal- ./splunk start->q(licence)->yes
password set
webUI url= http://IP:8000
----------------------------------------
devices(data source)->(raw data)->splunk(data input)(check source type->checks log type(parsing))->index(storage(contains metadata+raw data))
index can be easily analysed
SPL-Search Processing Language
index=web | stats sum(bytes) BY clientip
